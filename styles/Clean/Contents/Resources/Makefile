# Makefile.in generated by unsermake
####################################

top_builddir = ../../../../..
top_srcdir = ../../../../..
srcdir = .
.FORWARDS: all install install-data install-exec clean check force-reedit

all:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/usr/local/unsermake/unsermake all

install:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/usr/local/unsermake/unsermake install

install-data:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/usr/local/unsermake/unsermake install-data

install-exec:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/usr/local/unsermake/unsermake install-exec

clean:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/usr/local/unsermake/unsermake clean

check:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/usr/local/unsermake/unsermake check

force-reedit:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/usr/local/unsermake/unsermake force-reedit

$(top_builddir)/kopete/styles/Clean/Contents/Resources/Makefile: $(top_builddir)/config.status $(top_srcdir)/kopete/styles/Clean/Contents/Resources/Makefile.in
	cd $(top_builddir) && $(SHELL) ./config.status kopete/styles/Clean/Contents/Resources/Makefile

all: $(top_builddir)/kopete/styles/Clean/Contents/Resources/Makefile
final: 
	@echo no programs available to recreate as final. Call in subdirs

.PHONY: all final

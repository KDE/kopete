dnl Only compile motionautoaway on Linux (needs video4linux)

AC_MSG_CHECKING([Checking if motionautoaway plugin should be compiled])

if test "x`uname`" = "xLinux"; then
  COMPILEMOTION=true
  AC_SUBST(COMPILEMOTION)
  AC_MSG_RESULT([Linux -> yes])
else
  COMPILEMOTION=
  AC_SUBST(COMPILEMOTION)
  AC_MSG_RESULT([NON Linux -> no])
fi

AM_CONDITIONAL(include_motionautoaway, test -n "$COMPILEMOTION")

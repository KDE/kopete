Index: 003_case_insensitive_jid.patch
===================================================================
--- 003_case_insensitive_jid.patch	(revision 513088)
+++ 003_case_insensitive_jid.patch	(working copy)
@@ -1,14 +0,0 @@
-Index: iris/xmpp-core/jid.cpp
-===================================================================
---- iris/xmpp-core/jid.cpp	(revision 469141)
-+++ iris/xmpp-core/jid.cpp	(working copy)
-@@ -233,6 +233,9 @@
- 		b = d;
- 	else
- 		b = n + '@' + d;
-+	
-+	b=b.toLower(); // JID are not case sensitive
-+	
- 	if(r.isEmpty())
- 		f = b;
- 	else
Index: iris/xmpp-core/jid.cpp
===================================================================
--- iris/xmpp-core/jid.cpp	(revision 513088)
+++ iris/xmpp-core/jid.cpp	(working copy)
@@ -235,6 +235,9 @@
 		b = d;
 	else
 		b = n + '@' + d;
+	
+	b=b.toLower(); // JID are not case sensitive
+	
 	if(r.isEmpty())
 		f = b;
 	else
